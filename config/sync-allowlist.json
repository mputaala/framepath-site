{
  "version": 1,
  "description": "Allowlist of source paths inside the dev-repo checkout that sync-content.mjs is permitted to read. Anything not on this list is invisible to the build. The dev-repo checkout's root is referenced as the sync-script's --dev-repo argument; every entry's 'source' is relative to that root.",
  "sources": [
    {
      "id": "policies",
      "source": "Documentation/Policies",
      "kind": "markdown-directory",
      "destination": "content/policies",
      "notes": "Privacy_Policy.md and any future policies. US-156 transform: parse YAML frontmatter via gray-matter; if absent, synthesise { title, effectiveDate } from the first H1 and the first '**Effective date:** ...' line. Strip lines starting with '<!-- internal:'. Neutralise markdown links whose host matches *.cloudfunctions.net / console.firebase.google.com / mputaala.github.io/Frame (anchor text preserved as plain text). Compute slug from H1 with a trailing ' Policy' stripped before kebab-casing, so 'Privacy Policy' -> 'privacy'. Fail the build on missing / empty source files or slug collisions."
    },
    {
      "id": "screenshots-final",
      "source": "Documentation/User Guide/Assets/Screenshots",
      "kind": "image-allowlist",
      "destination": "public/screenshots",
      "notes": "Curated PNG screenshots. The 'source' here is a security boundary — every entry in config/screenshot-allowlist.json must resolve under this path. The dev repo's actual screenshots live under nested per-feature subdirectories (e.g. '01 — Welcome to FramePath/Final/', '10 — Storyboarding/Final/') rather than a single flat Final/ directory; the boundary therefore broadens to the parent. Sharp-driven AVIF/WebP/PNG triplet pipeline + 400w WebP thumbnail per entry lands in US-158; the per-screenshot allowlist drives selection."
    },
    {
      "id": "features",
      "source": "Documentation/Features",
      "kind": "markdown-directory-with-frontmatter-gate",
      "destination": "content/features",
      "notes": "Only files with frontmatter `published: true` are copied. Frontmatter gate lands in US-159."
    },
    {
      "id": "help-book",
      "source": "Documentation/Guides/Apple Help Book.md",
      "kind": "markdown-file",
      "destination": "content/help/index.mdx",
      "notes": "Apple Help Book renders as a single page with H2-anchored sections. Transform lands in US-160."
    }
  ]
}
